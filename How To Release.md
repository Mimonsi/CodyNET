## Release Steps
1. Open Version number in version.json and update it to the new desired version (e.g. 0.0.6)
2. Commit the changes
3. Create a tag with the same version number (e.g. v0.0.6)
4. Push the commit and the tag to the remote repository
5. GitHub actions will automatically create the release